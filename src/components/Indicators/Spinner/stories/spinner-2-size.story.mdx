import { Meta, Preview, Canvas, Props, Stories, Story, Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable, ColorPalette, ColorItem, IconGallery, IconItem } from '@storybook/addon-docs/blocks';
import Spinner from '../spinner.js'


<Meta title="Components|Indicators/Spinner/" component={Spinner} />

# Size
<Description>
  Use the `size` property to specify a small, medium, or large Spinner.
</Description>

<Preview>
  <Story name="Size" style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'space-evenly' }}>
    <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'space-evenly' }}>
      <Spinner size="sm"     label="small" />
      <Spinner size="medium" label="medium" />
      <Spinner size="large"  label="large" />
    </div>
  </Story>
</Preview>