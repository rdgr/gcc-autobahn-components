import { Meta, Preview, Canvas, Props, Stories, Story, Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable, ColorPalette, ColorItem, IconGallery, IconItem } from '@storybook/addon-docs/blocks';
import Spinner from '../spinner.js'


<Meta title="Components|Indicators|Spinner|" component={Spinner} />

# Labels
<Description>
  Text can be displayed with the Spinner with the `label` property. The text can be displayed below or beside the Spinner by setting the `inline` property to `true` or `false`.
</Description>

<Preview>
  <Story name="Labels">
    <div style={{ display: 'flex', alignItems: 'flex-start', justifyContent: 'space-evenly' }}>
      <Spinner size="large" label="Inline" inline="true" />
      <Spinner size="large" label="Not Inline" color="#00A895" />
    </div>
  </Story>
</Preview>