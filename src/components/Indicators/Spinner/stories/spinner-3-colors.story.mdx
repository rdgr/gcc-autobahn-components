import { Meta, Preview, Canvas, Props, Stories, Story, Title,
  Subtitle,
  Description,
  Primary,
  ArgsTable, ColorPalette, ColorItem, IconGallery, IconItem } from '@storybook/addon-docs/blocks';
import Spinner from '../spinner.js'


<Meta title="Components|Indicators|Spinner|" component={Spinner} />

# Colors
<Description>
  Use the `background` and `color` properties to change the colors of the Spinners channel and indicator.
</Description>

<Preview>
  <Story name="Colors">
    <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'space-evenly' }}>
      <Spinner size="large" color="#00A895" />
      <Spinner size="large"  />
      <Spinner size="large" background="#333333" color="#FFCC00" />
    </div>
  </Story>
</Preview>